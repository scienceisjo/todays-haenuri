# 클로드에게 전달할 프롬프트

너는 이 학교 알리미 앱의 다음 개발자야. 첨부한 `todays-school` 소스를 읽고, 실제 학교에서 사용할 수 있도록 이어서 개발해줘.

먼저 `README.md`, `docs/REQUIREMENTS.md`, `docs/IMPLEMENTATION.md`, `docs/TEST_REPORT.md`와 실제 코드를 읽어줘.
텍스트 묶음인 `CLAUDE_SOURCE.txt`를 받은 경우 경로별 파일을 복원해서 작업해줘. 그 파일 안의 복원 규칙을 따라줘.

## 사용자와 이미 정한 방향

나는 한국의 중학교 교사야. Windows 컴퓨터를 켰을 때 바탕화면 앱처럼 열리는 교직원용 「오늘의 학교」를 만들고 있어.
선생님들이 아침에 오늘 필요한 정보를 한 화면에서 빠르게 파악하는 것이 목적이야.

첫 화면에는 다음 내용을 보여줘.
- 날짜·요일
- 오늘 급식
- **금일 급식지도: 식당 입구 1명, 식당 내부 1명. 정확히 두 자리로 고정**
- 부서별 안내·교직원 전체 공지
- 행사·회의·방송 안내
- 제출 기한
- 학사 일정
- 창체의 날 교육 내용과 첨부 시간표

교직원 모두가 직접 정보를 입력할 수 있어야 해. 즉시 게시·예약 게시·임시저장을 지원하고, 기본 승인 대기는 없어.
일반 교직원은 본인이 작성한 자료를 수정하고, 관리자는 전체를 관리해.
교무기획에서 보내는 창체 시간표 파일을 올리고 해당 날짜에 확인할 수 있게 해야 해.
긴 내용은 카드 요약과 상세 창으로 나누되, 핵심 정보는 처음 화면에서 읽을 수 있게 해줘.

## 이미 만들어진 기반

- 프런트엔드: 한국어 HTML/CSS/JavaScript, PWA 구성
- 서버: Node.js 24+, 내장 HTTP 서버와 SQLite
- 외부 npm 의존성 없음
- 서버 데이터베이스 저장과 다중 계정 공유, 실제 로그인·권한 검사
- 공지와 일정 CRUD, 예약 공개·종료·임시저장, 읽음 처리
- 급식·급식지도 CRUD, 고정 두 자리·중복 방지·수정 충돌 방지
- 비공개 첨부 업로드·다운로드, 파일별 열람 권한
- 기본 관리자 기능, 데모/운영 데이터 분리, 서버 통합 테스트 17개

SQLite는 초안을 실행하고 검증하기 위해 선택한 것이야. 원래 Supabase 같은 백엔드도 후보였지만, 학교 계정·배포 서비스는 아직 결정되지 않았어.
지금은 외부 서비스 가입 없이 `npm run demo`로 동작해. 이 동작을 보존하면서 작업해줘.

## 먼저 할 일

1. `npm run check`, `npm test`, `npm run demo`를 실행하고 실제 코드를 확인해.
2. 브라우저에서 관리자/교직원 화면, 오늘/내일/이번 주, 안내 작성과 파일 첨부, 급식지도 변경을 점검해.
3. 실제 Windows에서 Node 실행, Edge 설치, 로그인 유지와 자동 시작을 검증해. 검증할 수 없는 환경이면 검증하지 못했다고 명시해.
4. 코드에서 발견한 오류를 먼저 고치고, 아래 후속 기능을 단계적으로 구현해.

## 다음 구현 순서

### A. 일상 입력을 편하게 만들기
- 급식지도 월간 표: 날짜마다 '식당 입구', '식당 내부' 두 칸을 표시하고 빠르게 수정.
- 기존 교직원 ID를 사용하며, 동명이인은 소속 부서로 구별.
- 대체 담당자는 해당 자리의 현재 담당자를 변경하고 변경 이력을 남김. 세 번째 지도를 추가하지 않음.
- 급식지도·학사 일정 엑셀/CSV 일괄 등록: 샘플 양식, 업로드 미리 보기, 날짜·동명이인·중복 검증, 저장 전 오류 표시.
- 중복 날짜는 조용히 덮어쓰지 말고 수정할 내용을 확인할 수 있게 함.
- 반복 일정: 매주·매월, 시작/종료, 특정 회차만 변경·취소, 휴업일 예외.
- 현재 일반 급식 입력은 중식 기준. 학교 급식 정보 자동 연동은 학교 식별 정보 확인 후 추가 가능.

### B. 파일과 게시 관리 보완
- 파일 첨부를 포함한 저장 실패 시 안내 본문이 먼저 공개되는 문제를 개선. 가능하면 파일 준비 후 게시를 확정하는 흐름으로 변경.
- 업로드 실패 후 재시도 시 공지나 파일이 중복 생성되지 않게 함.
- PDF·이미지 미리 보기를 실제 브라우저에서 검증.
- HWP/HWPX·엑셀·워드는 우선 원본 다운로드를 유지. 변환기를 연결하지 않고 미리 보기가 된다고 표시하지 않음.
- 급식지도 예약 목록과 미지정 날짜를 관리자가 쉽게 확인.
- 부서 수정·삭제, 앱 표시 순서·학교 로고 설정, 변경 전후 이력·백업 복원은 아직 없음. 필요한 것부터 구현.

### C. 학교 운영 환경 준비
- 하나의 중앙 서버에 데이터와 파일을 저장하고, 각 교직원 컴퓨터는 같은 주소로 접속.
- 중앙 주소, HTTPS, 정확한 APP_ORIGIN, Secure 쿠키, 데이터 영구 저장 경로를 설정.
- 서버 재시작/장애 복구, 데이터와 첨부파일 백업 및 복원 절차를 마련.
- 기존 학교 계정 사용 여부를 확인한 뒤 Microsoft 365/Entra 연동 또는 현재 관리자 발급 로그인 방식으로 구성.
- 학교 계정 인증 성공과 교직원 사용 권한을 구분하고, 실제 허용된 교직원만 접근하도록 함.
- 기존 데이터와 작성자 연결을 보존해. 데이터베이스 구조 변경은 새 마이그레이션으로 수행.

## 유지할 원칙

- 이미 만든 화면과 데이터 저장 구조를 읽기 전에 처음부터 새 앱으로 교체하지 마.
- localStorage를 교직원 공용 데이터 저장소로 사용하지 마.
- 로그인 화면만 있고 실제 API는 공개되는 구현을 하지 마.
- 작성자·관리자 권한과 첨부파일 열람 권한은 서버에서 검사해.
- 한국 시간 Asia/Seoul을 기준으로 날짜와 예약 시각을 처리해.
- 마감일과 게시 종료일은 별개야. 게시 종료가 지난 공지는 기본 화면에서 숨기고 관리 화면에서 보존해.
- '담당자 미지정'과 '식당 입구/식당 내부' 두 자리는 유지해.
- 기존 데모 계정은 데모에서만 사용해. 운영용 계정에 알려진 공통 비밀번호를 심지 마.
- 프로그램을 계속 수정할 수 있게 역할별 파일로 분리해도 되지만, 선택한 기술과 동작을 불필요하게 모두 교체하지 마.
- 배포 계정이나 학교 Microsoft 설정처럼 실제 정보가 필요한 부분만 구체적으로 질문하고, 그 전까지 가능한 구현은 계속해.
- 검증한 기능과 아직 연결되지 않은 기능을 구분해서 알려줘.

내가 원하는 결과는 작동하는 앱의 다음 버전이야. 기획 설명은 짧게 하고 실제 코드 수정부터 시작해줘.
