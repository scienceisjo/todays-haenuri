# 변경 이력

날짜는 한국 표준시(Asia/Seoul) 기준입니다.
이 앱은 **교사용 학교 알림장**입니다. 선생님 각자의 PC를 켜면 자동으로 열려 아침에 필요한 정보를 한 화면에서 보는 것이 목적입니다(전자칠판용이 아님).

## v1.2.0 — 2026-09-21 · PC 켜면 자동 실행

- `public/autostart-on.bat` / `autostart-off.bat`: 시작 프로그램 폴더(shell:startup)에 Edge(없으면 Chrome·Whale)를 `--app=` 앱 창으로 여는 바로가기 ‘Todays-Haenuri’를 만들고/지운다. 관리자 권한 불필요, 브라우저를 못 찾으면 기본 브라우저용 .url 바로가기. UTF-8+`chcp 65001`, CRLF 고정(.gitattributes `*.bat -text`). 스크래치 폴더로 켜기/끄기 두 번씩 실행해 바로가기 대상·인수 확인.
- 사이드바 **‘PC 켜면 자동 실행’** 안내창: 파일 내려받기 링크·실행 순서·“로그인 유지” 안내·설치 앱의 “로그인 시 앱 시작” 대안.

## v1.1.1 — 2026-09-21 · 공용 계정 지원 + 비밀번호 규칙 완화

- **비밀번호 최소 길이 12자 → 6자**(Supabase 기본값과 같음): 가입·변경·재설정 폼과 오류 문구. 대시보드 Authentication → Providers → Email 의 "Minimum password length" 를 올려 두었다면 6으로.
- **전 교직원 공용 계정** `supabase/shared-account.sql`: 명단에 `hnralimi@haenuri.test`(권한 교직원·부서 없음) 추가 + 설정 `shared_login_email`. 계정 자체는 앱 가입 화면이나 대시보드 Add user 로 만든다(비밀번호는 파일에 두지 않음).
  - 이 계정으로 들어오면 사이드바에 ‘전 교직원 공용 계정’, **비밀번호 변경 메뉴 숨김**(누군가 바꿔 버리는 사고 방지), 안내 등록 폼 앞쪽에 **담당 부서 선택**(기본 ‘부서 없음 · 교직원 공통’).
  - 설정 읽기 정책: `shared_login_email` 은 로그인한 교직원만 읽음(로그인 전 화면에 노출 안 함). schema.sql 도 같이 갱신.
- 한계: 공용 계정으로 올린 글은 작성자가 모두 같은 이름이고, 그 계정으로 들어온 누구나 그 글을 고치거나 지울 수 있다. 변경 기록에도 개인이 남지 않는다.

## v1.1.0 — 2026-09-21 · 가벼운 안내 등록 + 2026 창체의 날 초기 자료

사용자 피드백: "공지 하나 올리기까지 지정해야 하는 게 너무 많고, 하나라도 비우면 빨간 오류가 뜬다."

### 바뀐 것 — 안내 등록
- **‘안내 등록’ 버튼이 바로 등록 폼을 연다**(무엇을 등록할까요? 선택창 제거). 급식·급식지도·파일 일괄 등록은 폼 아래 ‘다른 등록’ 링크와 각 카드의 등록 버튼으로.
- **폼 구성**: 유형 칩(공지사항·행사·회의·제출 기한·방송 안내·학사 일정·창체 시간표) → 제목 → 내용(선택) → [일정 유형만] 날짜·시작 시간·장소 한 줄 → 첨부(선택). **필수 입력은 제목 하나.** 날짜는 보고 있던 날짜가 미리 채워짐.
- **‘세부 설정’으로 접어 둔 것**: 공지 구분(기본 전체 공지)·담당 부서(기본 내 부서)·열람 대상·중요도·종료 날짜/시간·링크·게시 방법(즉시/예약/임시저장)·예약 시각·게시 종료·반복. 수정할 때 값이 있으면 자동으로 펼쳐짐.
- **빈 칸 오류 해결**: 비워 둔 선택 항목('')이 그대로 DB 로 가서 날짜·시간·부서 제약에 걸리던 문제. 저장 전에 `validateRecord`(core.js)로 정리해 null·기본값으로 보낸다(공지사항은 날짜 없이 저장). 유형별 문구·마감/방송 날짜 라벨·버튼 이름(게시하기/예약하기/임시저장)이 선택에 따라 바뀜.
- 홈 **창체의 날 시간표 칸**: 그날 시간표가 없으면 **다음 창체의 날을 D-day 와 교시 운영 요약**으로 보여준다.

### 추가 — 초기 자료 `supabase/seed-2026.sql`
- 출처: 2026학년도 학사일정 상세(교사용, 2.12.) PDF. 관리자 가입 후 SQL Editor 에서 한 번 실행(다시 실행해도 중복되지 않음, 앱에서 삭제한 항목은 되살리지 않음).
- **창체의 날 8회**(창체 시간표 유형, 교시별 운영 내용) + 연간 요약 공지 1건, **학사 일정 21건**(시험·성적이의신청·요일 바꿔 수업·체험일·예술제·방학/종업), **휴업일 16일**(반복 일정·급식지도 양식에서 자동 제외).

### 테스트
- `tests/record-light.test.mjs`(2): 빈 칸 정리·일정 유형 날짜 규칙. UI 스모크에 유형 칩 6개·필수 입력 1개·날짜 줄 숨김/표시 검사 추가. 총 15개 통과.

## v1.0.0 — 2026-09-21 · GitHub Pages + Supabase 로 이식

선생님이 서버 없이 운영할 수 있도록 데이터 계층을 **Supabase**(Postgres·Auth·Storage)로 옮기고, 화면은 **GitHub Pages** 정적 배포로 바꿨습니다. 화면·기능·규칙은 v0.3.0 을 그대로 가져왔습니다.

### 바뀐 것
- **설치 SQL** `supabase/schema.sql`: 표(부서·명단·교직원·안내·시리즈·읽음·첨부·급식·급식지도·휴업일·설정·기록), **RLS 정책**(작성자·관리자·공개 조건·부서 열람), 저장소 버킷(비공개 attachments·공개 branding)과 파일 정책, 가입 규칙(명단 기반, 관리자 이메일/첫 가입자), **함수**: `import_duties`·`import_schedule`(원자적 일괄 저장), `create_series`(반복), `update_record`·`delete_record`(버전 충돌 검사 + 회차 범위), `can_register`, `server_time`. 트리거로 버전 증가·삭제/복구 규칙·재직자 검사·변경 기록.
- **로그인**: Supabase Auth 이메일·비밀번호. 관리자가 **교직원 명단**(이메일·이름·부서)을 등록하면 그 이메일만 가입 가능. 비밀번호 재설정 메일, "이 컴퓨터에서 로그인 유지" 선택.
- **데이터 계층** `public/lib/data.js`: 화면은 이 모듈만 호출. 낙관적 잠금(version), 첨부 서명 URL, 로고 공개 URL, 일괄 등록 미리 보기(브라우저)→RPC 저장.
- **순수 로직 이식** `public/lib/core.js·series.js·import.js`: Node 전용 API 제거(Buffer→DataView, zlib→DecompressionStream 주입). 같은 모듈을 Node 테스트가 검증.
- **정적 배포**: 상대 경로, 로컬 vendor(supabase-js 2.116), `<meta>` CSP, 서비스워커 scope 기준 캐시, GitHub Actions 배포 워크플로, 로컬 미리보기 `npm run preview`.
- **아이콘**: `scripts/build-icons.mjs` 가 branding/logo.png 에서 logo.png·icon-192/512 생성(해누리 엠블럼). 사이드바 "바탕화면 앱으로 설치" 버튼(beforeinstallprompt).
- **관리자 화면**: 가입한 교직원(이름·부서·권한·사용 여부), 교직원 명단(개별/붙여넣기 추가·빼기), 부서 삭제.
- 옛 Node+SQLite 서버는 `_legacy/` 로 이동(참고용). 데모 계정·데모 데이터 제거.

### 아직 확인하지 못한 것
- 실제 Supabase 프로젝트에서의 SQL 실행·권한·함수 동작(사용자가 SQL 을 실행하면 이어서 검증), GitHub Pages 배포 실물, Edge 설치·자동 시작.

## v0.3.0 — 2026-09-21

### 추가 — CSV·엑셀 일괄 등록 (요구사항 A)
- **급식지도 일괄 등록**: 급식지도 월간표·등록 선택창에서 진입. 그 달 수업일(월~금, 휴업일 제외)이 채워진 **CSV 양식 내려받기** → 이름 채워 올리기 → **미리 보기** → 저장.
  - 열: 날짜 / 식당 입구 / 식당 내부 / 비고 / (선택) 공개 시각. 머리글 이름은 몇 가지 표기를 자동 인식.
  - 담당자는 **이름**으로 적고, **동명이인은 `이름(부서)`**로 구별. 이메일도 허용. 빈 칸은 ‘담당자 미지정’.
  - 행마다 검증: 날짜 형식(연도 필수), 없는 교직원, 동명이인, 같은 사람 두 자리, 파일 안 같은 날짜, 휴업일 경고.
  - **이미 등록된 날짜는 조용히 덮어쓰지 않는다.** 미리 보기에 ‘현재 → 파일’ 비교를 보여주고 ‘건너뛰기 / 파일 내용으로 갱신’을 고른다(갱신은 작성자·관리자만).
  - 오류 행이 있으면 기본은 **아무것도 저장하지 않음**(원자적). ‘오류 행은 제외하고 저장’을 켜면 나머지만 저장. 담당자가 빈 날짜는 기본 건너뛰고, 선택 시 ‘담당자 미지정’으로 등록.
- **학사 일정 일괄 등록**: 전체 일정(달력)·등록 선택창에서 진입. 열: 날짜 / 종료 날짜 / 유형 / 제목 / 내용 / 장소 / 시작·종료 시간 / 중요도.
  - 유형: 학사 일정 · 행사·회의 · 방송 안내 · 제출 기한 · 창체 시간표 · 공지사항 · **휴업일**(관리자만). 제목이 ‘예시:’로 시작하는 양식 예시 행은 자동으로 건너뜀.
  - 중복 기준은 같은 유형+제목+날짜. 파일 안 중복도 오류로 표시.
- **파일 형식**: CSV(UTF-8 BOM·UTF-8·**CP949(한국어 엑셀 기본 CSV)**·UTF-16 자동 감지, 쉼표·탭·세미콜론 자동 감지)와 **엑셀 .xlsx**(외부 라이브러리 없이 첫 시트를 직접 읽음: 공유 문자열·숫자·**엑셀 날짜 일련번호**·시간 분수). 옛 .xls는 안내 후 거절.
- 날짜는 `2026-10-05`, `2026.10.5`, `2026년 10월 5일`, `20261005`, 엑셀 날짜 셀을 모두 인식하되 **연도가 없는 `10/5`는 오류**로 알려 실수를 막는다. 시간은 `14:30`, `오후 2:30`, `14시 30분`, 엑셀 시간 셀.
- 한 번에 500행까지, 파일 10MB 이하. 모든 저장은 서버에서 다시 검증하고 하나의 트랜잭션으로 처리.

### 추가 — 반복 일정 (요구사항 A)
- 새 일정 등록 폼의 **‘반복 일정으로 등록’**: 매주(요일 여러 개 가능)·매월, 간격(격주·격월), 종료 날짜 또는 횟수(최대 200회·2년), **휴업일 건너뛰기**.
- 회차마다 **개별 일정**으로 만들어져(`series_id`로 묶임) 기존 화면·권한·읽음 처리가 그대로 적용된다. 달력에는 ↻ 표시, 상세에는 ‘반복 · n/N회차’.
- **특정 회차만 변경·취소**: 회차를 열어 평소처럼 수정·삭제. 수정 창의 **적용 범위**(이 회차만 / 이 회차부터 이후 모두 / 시리즈 전체)로 이후 회차의 제목·내용·시간·장소·중요도를 한 번에 바꾸되 **각 회차의 날짜는 보존**. 삭제 창도 같은 범위 선택.
- 반복 일정에는 첨부를 붙일 수 없고(등록 후 회차별 추가), 공지사항(날짜 없음)은 반복 불가.

### 추가 — 학교 브랜딩(앱 이름·로고)과 해누리중학교 부서
- **앱 이름**이 설정값이 됐다(`app_name`). 데모·설치 기본값은 학교명에서 만든 **「오늘의 해누리」**이며 학교 설정에서 바꿀 수 있다. 사이드바·로그인·브라우저 탭 제목·설치 앱(manifest) 이름에 모두 쓰인다.
- **학교 로고**: 학교 설정 → 학교 로고에서 PNG/SVG(1MB 이하)를 올리면 사이드바·로그인·즐겨찾기 아이콘이 바로 바뀌고, **정사각형 192px 이상 PNG면 Windows 설치 앱(PWA) 아이콘까지** 로고로 바뀐다(manifest 동적 생성). `/logo`는 로그인 전에도 보이도록 공개 경로이며, 스크립트가 든 SVG는 거절하고 SVG 응답에는 sandbox CSP를 붙인다. 로고 버전(`logo_version`)으로 브라우저 캐시를 갱신한다.
- 프로젝트 `branding/logo.png`(또는 .svg)를 두면 `npm run demo`·`npm run setup`이 그 파일을 첫 로고로 설치한다(`branding/README.md`). 파일이 없으면 데모는 해 문양 임시 마크를 쓴다. **공식 해누리 엠블럼(`11_웹앱개발/해누리중 로고.png`, 688×687 RGB)을 `scripts/logo.mjs`로 배경 투명·680×680 정사각형으로 다듬어 `branding/logo.png`에 설치했다** — 사이드바·로그인·탭·설치 아이콘(manifest 680×680)에 적용 확인.
- 부서를 해누리중학교 실제 구성 **14개**로 교체: 교무업무지원팀·교육연구지원팀·학생생활부·체육안전부·과학정보부·창의인성부·1·2·3학년부·진로진학부·상담복지부·학력신장부·행정실·시설관리과(`server/seed.mjs`의 `DEPARTMENTS`, 설치 스크립트도 공유).

### 추가 — 휴업일
- 학교 설정 → **휴업일** 카드(관리자): 날짜·이름 추가/삭제. 학사 일정 일괄 등록의 ‘휴업일’ 행으로도 등록.
- 반복 일정 생성과 급식지도 양식에서 자동 제외, 달력·미니 달력·주간·급식지도 월간표에 표시. 월간표의 ‘담당자 미지정’ 집계에서 제외.

### 데이터
- **마이그레이션 002**: `record_series`, `holidays` 표와 `records.series_id/series_index` 열 추가. 기존 데이터·작성자 연결은 그대로 유지되며 앱 시작 시 자동 적용(확인 완료).
- 마이그레이션 러너를 파일 순서(001, 002, …) 자동 적용으로 일반화. JSON 내보내기에 `record_series`, `holidays` 포함(schema_version 2).

### 테스트
- 새 테스트 파일 `tests/import.test.mjs`(10개)·`tests/recurring.test.mjs`(5개)·`tests/branding.test.mjs`(4개), 공용 `tests/helpers.mjs`(테스트용 최소 .xlsx 생성기 포함). 화면 스모크 테스트에 일괄 등록 미리 보기·반복 설정·범위 선택·휴업일 렌더링 추가. **총 38개 통과**.
- 브라우저에서 실제로 실행: CSV 파일 올리기 → 미리 보기(diff·오류·건너뜀) → 갱신+오류 제외 저장, 반복 일정 폼 등록(월·수 4회), 2회차 이후 장소 일괄 변경, 시리즈 전체 취소, 학사 일정 CSV(휴업일 포함) 등록, 휴업일 카드, **로고 PNG 업로드 → 사이드바·파비콘 즉시 교체(512px) → 기본 아이콘 되돌리기**.

### 참고
- `node scripts/logo.mjs "원본.png" [출력.png]`: 외부 라이브러리 없이 PNG를 읽어 가장자리와 이어진 흰 배경을 투명하게 만들고 그림을 정사각형 가운데에 맞춘다(8비트 RGB/RGBA/회색, 비인터레이스).
- 급식지도 양식 열 이름 등 인식 규칙은 `server/import.mjs`의 `HEADERS`에서 늘릴 수 있다.
- 일괄 등록 미리 보기 창은 넓은 창(`dialog.wide`)으로 연다.

## v0.2.0 — 2026-09-21

이어받은 클로드가 초안(v0.1.0)을 실행·검증하고 후속 기능 일부를 구현한 버전입니다.
`npm run demo` 동작과 외부 의존성 0, SQLite 저장 구조를 그대로 유지했습니다.

### UI/UX — Apple 스타일 리디자인
- `public/styles.css`를 Apple 디자인 언어로 전면 재작성(클래스명·마크업·기능은 그대로 유지, 시각 언어만 교체).
- 배경 `#f5f5f7` + 순백 카드 + 은은한 그림자, 시스템 블루(#0071e3) 단일 강조색, iOS식 세그먼트·필 버튼·틴트 태그.
- **반투명(frosted) 사이드바**(backdrop blur), 원형 아이콘 버튼, 둥근 22px 다이얼로그 + 블러 백드롭, 로그인 화면을 밝은 그라데이션 히어로로 교체.
- 타이포는 `-apple-system`→Pretendard→Segoe UI/맑은 고딕 순의 **로컬 폰트 스택**(CSP `font-src 'self'` 유지, 외부 폰트·오프라인 원칙 보존).
- 데스크톱·다이얼로그·로그인·모바일(375px)에서 브라우저로 확인. (다크 모드는 미적용 — 필요 시 토큰 기반으로 추가 가능)

### 추가 — 급식지도 월간 관리표 (요구사항 A)
- 사이드바 **급식지도** 메뉴와 급식지도 카드의 **월간표** 버튼으로 진입.
- 수업일(월~금) 한 달을 표로 표시하고, 주말·휴업일은 지도가 등록된 경우에만 표시.
- 날짜마다 **식당 입구 / 식당 내부** 두 자리(고정)와 상태(지정 완료 / 자리 비어 있음 / 예약 / 미등록)를 함께 표시.
- 상단에 **담당자 미지정 일수**와 **예약 건수** 집계, **‘담당자가 빈 날짜만 보기’** 필터.
- 빈 날짜의 **담당자 지정** 버튼은 그 날짜가 채워진 등록 폼을 연다(세 번째 자리는 추가하지 않음).
- 대체 담당자 변경 시 비고(사유)를 표에 함께 노출. 저장 권한은 서버가 다시 검사.

### 개선 — 첨부 포함 안내의 원자적 게시 (요구사항 B)
- 첨부가 있는 **새 안내**는 먼저 비공개 초안으로 저장하고, 파일을 모두 올린 뒤, 마지막에 목표 상태로 **게시를 확정**한다.
- 업로드가 중간에 실패해도 **본문이 첨부 없이 먼저 공개되지 않는다.** 실패 지점에서 다시 저장하면 남은 파일만 이어 올린 뒤 게시를 확정한다.
- 중복 방지: 초안 생성 1회, 파일은 이름·크기·수정시각 키로 건너뜀, 게시 확정 1회.
- 첨부 업로드 응답에 갱신된 레코드 `version`을 포함해, 게시 확정 PATCH가 정확한 버전으로 낙관적 잠금을 통과하도록 함.
- 실패 안내 문구를 ‘비공개(임시) 보관 중’ 상태에 맞게 수정.

### 테스트
- 서버 통합 테스트 17 → **18개**(초안→업로드→게시 확정 순서와 첨부 version 반환 검증 추가).
- 화면 렌더링 스모크 테스트에 **급식지도 월간표** 렌더링 검사 추가(총 **19개** 통과).
- Windows 11 / Node 24.15 에서 `npm run check`, `npm test` 통과.
- 데모 서버를 실제 브라우저로 조작해 로그인·대시보드·주간·급식지도 변경·월간표·이미지 미리 보기 응답을 확인(자세한 범위는 `docs/TEST_REPORT.md`).

### 유지
- 급식지도 두 자리 고정, Asia/Seoul, 마감일≠게시종료, 서버 측 권한·첨부 열람 검사, 데모/운영 데이터 분리, 마이그레이션 001 스키마 불변.

### 다음(미구현)
- 급식지도·학사 일정 엑셀/CSV 일괄 등록(샘플 양식·미리 보기·검증), 반복 일정.
- 부서 수정·삭제, 로고·카드 표시 순서, 백업/복원.
- 중앙 서버·HTTPS·APP_ORIGIN·Secure 쿠키·영구 저장 경로, Microsoft 365/Entra 연동.
- 실제 급식 자동 연동(학교 식별 정보 확인 후).

## v0.1.0 — 인계 초안

- Node.js 24 내장 HTTP + `node:sqlite`, 빌드 없는 프런트, 외부 npm 의존성 0.
- 로그인·권한, 공지/일정 CRUD·예약·임시저장·읽음, 급식·급식지도(두 자리 고정), 비공개 첨부, 관리자 기능, 데모/운영 분리, 서버 통합 테스트 17 + 렌더링 1.
